# Ejecutar el backend (puerto por defecto 4000)
PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# PostgreSQL (BD: bosco)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=tu_password
PGDATABASE=bosco

# JWT
JWT_SECRET=cambia-este-secreto
JWT_EXPIRES_IN=8h
